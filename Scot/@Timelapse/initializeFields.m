function obj=initializeFields (obj)
    % initializeFields --- Empty function to avoid an error when this is called using a timelapse object
    %
    % Synopsis:  obj = initializeFields (obj)
    %
    % Input:     obj = an object of a Timelapse class
    %
    % Output:    obj = an object of a Timelapse class

    % Notes: All the important timelapse initiation functions are done by
    % the constructors

end